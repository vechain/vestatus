{
  "name": "block-measurement",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "rm -rf dist;tsc -p .",
    "prepare": "npm run build",
    "dev": "ts-node ./src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"        
  },
  "bin": {
      "block-measure": "dist/index.js"
  },
  "author": "qianbin",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^13.9.2",
    "ts-node": "^8.7.0",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "axios": "^0.19.2",
    "influx": "^5.5.1"
  }
}
